// YEW HONG PART (Booking Management)
void bookingMenu() {
	int choice;
	do {
		cout << "==========================================" << endl;
		cout << "            Booking Management     " << endl;
		cout << "==========================================" << endl;
		choice = getMenuChoice(put lowest choice, put highest choice);
		switch (choice){
		case 1: first function
			break;
		case 2:....
		}

	} while (choice != 0); 
}

#include <iostream>
#include <string>
#include <iomanip>
using namespace std;

//Booking Struct (Yew Hong)
struct Booking {
	string activity;
	float times[10]; //define times array
};

void booking(Booking b, Payment p) { //Payment p from Wei Yuan stuct
	while (true) {
		int service;
		char determine;
		cout << "Current available pet grooming service :\n";
		cout << "1.Bath & Brush \n2.Nail Clipping \n3.Skin & Coat Treatment \n4.De-shedding Treatment\n";
		cout << "Please enter the number of service you want to book :";//asking user to select the service they prefer
		cin >> service;

		//return activity of user choosing
		if (service == 1) {
			b.activity = "Bath & Brush";
			for (int i = 0; i < 10; i++) {
				cout << "\nPlease enter the booking time in 24-hour clock (e.g. 15.00) :";
				cin >> b.times[i];
				cout << "\nWould you like to continue booking other times?" << endl;
				cout << "Press 1 to cancel, any other key to continue :" << endl;
				cin >> determine;
				if (determine == 1)
					break;
			}
		}


		else if (service == 2)
			b.activity = "Nail Clipping";

		else if (service == 3)
			b.activity = "Skin & Coat Treatment";

		else if (service == 4)
			b.activity = "De-shedding Treatment";

		else {
			cout << "Invalid, please try again." << endl;
		}
	}

}
	}

	
}
