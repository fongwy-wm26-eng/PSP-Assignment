void serviceMenu() {
	int choice;
	do {
		cout << "=========================================" << endl;
		cout << "      hi Kai Jie     " << endl;
		cout << "=========================================" << endl;
		choice = getMenuChoice(put lowest choice, put highest choice);
		switch (choice) {
		case 1: first function
			break;
		case 2:....
		}

	} while (choice != 0); 
}
